/*
题目描述：
已知N个元素的队列（N>0），每次进行以下操作：
（1）出列队首元素，
（2）再出列队首元素，并将它从队尾入列。
进行N-1次之后，队列中只剩下一个元素，输出该元素。

输入样式：
7
1 2 3 4 5 6 7

说明：
第一行为队列元素个数；
第二列为队列中的各个元素，用空格分开。

输出样式：
6

说明：
最后留在队列中的元素
*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Hello world!\n");
    return 0;
}
